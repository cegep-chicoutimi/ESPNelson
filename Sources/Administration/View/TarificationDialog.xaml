<Window x:Class="Administration.View.TarificationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        x:Name="label_PricingManagement">

    <Grid Margin="20">
        <StackPanel>
            <Label x:Name="label_Price"/>
            <TextBox Text="{Binding Tarification.Prix}" Margin="0,0,0,10"/>

            <Label x:Name="label_DurationMin"/>
            <TextBox Text="{Binding Tarification.DureeMin}" Margin="0,0,0,10"/>

            <Label x:Name="label_DurationMax"/>
            <TextBox Text="{Binding Tarification.DureeMax}" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="label_Save" Command="{Binding EnregistrerCommand}" Margin="0,0,50,0"/>
                <Button x:Name="label_Cancel" Command="{Binding AnnulerCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
